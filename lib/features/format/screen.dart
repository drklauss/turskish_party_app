import 'package:flutter/material.dart';

class FormatScreen extends StatelessWidget {
  const FormatScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Страница с форматом мероприятия"),
    );
  }
}
